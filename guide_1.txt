### 1. Инициализация проекта в Git

Для этого мы в папке с нашим проектом (пока что она может быть пустая) мы должны выполнить команду `git init`. В папке создаться файл .git и его можно сущестование можно проверить командой `ls -la` для Linux.

### 2. Создадим свой первый файл
